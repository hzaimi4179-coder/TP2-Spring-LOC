
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- component-scan will pick up @Component beans in the specified packages -->
    <context:component-scan base-package="com.example.dao,com.example.metier"/>

    <!-- Explicit bean definitions to ensure DaoImpl2 is used for XML configuration -->
    <bean id="dao" class="com.example.dao.DaoImpl"></bean>
    <bean id="dao2" class="com.example.dao.DaoImpl2"></bean>

    <!-- Metier configured to use dao2 by setter injection -->
    <bean id="metier" class="com.example.metier.MetierImpl">
        <property name="dao" ref="dao2"/>
    </bean>

</beans>
